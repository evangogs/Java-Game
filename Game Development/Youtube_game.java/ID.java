/*creates enumeration on either a player or a enemy so these can be detected in game object array,
 * so you can simply go "if ID == player();"
 */
public enum ID {

    Player(),
    BasicEnemy(), 
    Trail();

}
